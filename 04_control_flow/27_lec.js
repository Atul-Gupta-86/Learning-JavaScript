// Iteration

// for loop


// If ever stuck in infiite loop  ---> press  ( " ctrl + c ") 


for (let index = 0; index < 10 ; index++) {
    const element = index;
    // console.log(element)
    
}

let arr_1 = ["Atul", "Ashish", "Aryan", "Asmit"]
for (let i = 0; i < arr_1.length; i++) {
    const element = arr_1[i];
    console.log(element)
}

// break and continue ;
