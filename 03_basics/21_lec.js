// Scopes.
// Generally {} ---> Curluy braces are scopes but not in case of object as that { } --> curly braces in object are object declearration not scope.
 

// Global scope inside windows in browser's console is different as compared to global scope in node environment.